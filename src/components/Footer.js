import React, { Component } from 'react';
import './Footer.css'

class Footer extends Component {
  render() {
    return (
      <footer>
        <small>Markdown Previewer</small>
        <small>Coded by Arthur Coelho using React</small>
      </footer>
    );
  }
}

export default Footer;